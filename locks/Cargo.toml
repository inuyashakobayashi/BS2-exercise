[package]
name = "locks"
version = "0.1.0"
authors = ["Student <vorname.nachname@hu-berlin.de>"]
edition = "2024"

[lib]
name = "locklib"

[dependencies]
mio = { version = "1.2", features = ["net", "os-poll"] }
